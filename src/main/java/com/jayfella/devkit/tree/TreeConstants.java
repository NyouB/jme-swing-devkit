package com.jayfella.devkit.tree;

public class TreeConstants {

  // Indicates that this node is a tree root. The "fake" rootNode and guiNode will be the only holders of this flag.
  public static final String TREE_ROOT = "TREE_ROOT";

  // a Spatial hint that this spatial cannot be deleted (e.g. the root nodes).
  public static final String UNDELETABLE_FLAG = "DO_NOT_DELETE";

  private static final String LIGHTS_COLLECTION_NAME = "Lights"; // a Light collection list name.
  private static final String CONTROLS_COLLECTION_NAME = "Controls"; // a Control collection list name.

}
